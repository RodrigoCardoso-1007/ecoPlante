import colors from "../../styles/colors";

const styles = {
  text: {
    fontWeight: 'bold',
    color: colors.primary.dark,
    fontSize: '48px',
    margin: '0px',
    textAlign: 'center'
  }
}

export default styles;