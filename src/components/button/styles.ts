import colors from "../../styles/colors";

const styles = {
  button: {
    width: '100%',
    backgroundColor: colors.primary.dark,
    borderRadius: '18px',
    fontWeight: 'bold',
    fontSize: '16px',
  }
}

export default styles;